spring.application.name=blog-api


# 数据库配置
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/BLOG?useSSL=false
spring.datasource.username=root
spring.datasource.password=qian102418

# MyBatis-Plus 配置
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# # JWT 配置（新增）
jwt.secret=your-256-bit-secret-key-which-should-be-very-long-and-secure-123456
jwt.expiration=3600
jwt.refresh-expiration=604800

# 图片上传配置
# 开发环境：保存到 resources/images 目录（项目运行时的目标目录）
upload.image.path=/Users/qian/project/blog/images
# 生产环境：使用外部绝对路径（可选，打包部署时修改）
# upload.image.path=/opt/blog-api/images

# 图片访问基础URL（配合静态资源映射）
upload.image.base-url=/images